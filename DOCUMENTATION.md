# NES エミュレーター コード解説ドキュメント

## 概要

このリポジトリは Python で実装された**最小構成の NES（ファミコン）エミュレーターコア**です。
CPU 実行に特化した教育・実験用の実装であり、iNES 形式の ROM を読み込んで 6502 CPU 命令の一部を実行できます。

---

## ディレクトリ構成

```
nes/
├── __init__.py      パッケージ公開インターフェース
├── __main__.py      CLI エントリーポイント
├── ines.py          iNES ROM ローダー
├── memory.py        メモリーマップ実装
└── cpu.py           6502 CPU コア
```

---

## モジュール詳細

### 1. `nes/__init__.py` — パッケージ初期化

```python
from .cpu import CPU
from .ines import INESRom
from .memory import Memory
```

`CPU`・`INESRom`・`Memory` の 3 クラスをパッケージの公開 API として外部に公開します。

---

### 2. `nes/__main__.py` — CLI エントリーポイント

`python -m nes <rom> [--steps N]` で実行するときのエントリーポイントです。

#### 処理フロー

```
ROM ファイルパス → INESRom.load() → Memory.with_rom() → CPU() → cpu.reset() → cpu.run()
                                                                                   ↓
                                                              停止後にレジスター状態を表示
```

#### 引数

| 引数 | 型 | 説明 |
|------|----|------|
| `rom` | str | iNES ROM ファイルのパス（必須） |
| `--steps` | int | 実行する最大 CPU ステップ数（省略時: 無制限） |

#### 出力例

```
Stopped after 1234 steps
A=00 X=FF Y=00 SP=FD PC=C000 STATUS=24
```

---

### 3. `nes/ines.py` — iNES ROM ローダー

#### クラス: `INESRom`

iNES 1.0 フォーマットの ROM ファイルを解析して PRG ROM と CHR ROM を取り出します。

```
iNES ファイルレイアウト:
┌──────────────────────┐ offset 0
│  ヘッダー (16 バイト) │
├──────────────────────┤ offset 16 (+ 512 if trainer)
│  トレーナー (任意)    │  512 バイト (flags6 bit2 が立っている場合のみ)
├──────────────────────┤
│  PRG ROM             │  16 KB × prg_banks バイト
├──────────────────────┤
│  CHR ROM             │  8 KB  × chr_banks バイト
└──────────────────────┘
```

#### ヘッダー解析ロジック

| バイト位置 | 内容 |
|-----------|------|
| 0–3 | マジックナンバー `NES\x1a`（検証に使用） |
| 4 | PRG ROM バンク数（1 バンク = 16 KB） |
| 5 | CHR ROM バンク数（1 バンク = 8 KB） |
| 6 | Flags6（bit2 = トレーナー有無） |

#### メソッド

| メソッド | 説明 |
|---------|------|
| `INESRom.load(path)` | ファイルを読み込み `INESRom` インスタンスを返すクラスメソッド |

#### 属性

| 属性 | 型 | 説明 |
|-----|----|------|
| `prg_rom` | `bytes` | プログラム ROM データ |
| `chr_rom` | `bytes` | キャラクター ROM データ（グラフィック用、本実装では未使用） |

---

### 4. `nes/memory.py` — メモリーマップ

#### クラス: `Memory`

NES の簡略化されたメモリーマップを実装します。PPU・APU・I/O レジスターは省略されています。

#### メモリーマップ

| アドレス範囲 | サイズ | 内容 |
|------------|-------|------|
| `0x0000`–`0x1FFF` | 8 KB | RAM 領域（実体は 2 KB、`% 0x800` でミラーリング） |
| `0x2000`–`0x7FFF` | — | 未実装（PPU/APU/I/O）、読み取りは常に `0x00` |
| `0x8000`–`0xFFFF` | 32 KB | PRG ROM 領域 |

#### メソッド

| メソッド | 説明 |
|---------|------|
| `Memory.with_rom(prg_rom)` | 2 KB の RAM と PRG ROM を持つインスタンスを生成するクラスメソッド |
| `read(addr)` | 指定アドレスから 1 バイト読み取る |
| `write(addr, value)` | 指定アドレスへ 1 バイト書き込む（ROM / I/O への書き込みは無視） |

#### PRG ROM マッピング詳細 (`_read_prg`)

- PRG ROM サイズが **16 KB（0x4000 バイト）** の場合: `addr % 0x4000` でミラーリング（`0x8000`–`0xBFFF` と `0xC000`–`0xFFFF` に同一データが見える）
- PRG ROM サイズが **32 KB（0x8000 バイト）** の場合: ミラーリングなし、直接マッピング

---

### 5. `nes/cpu.py` — 6502 CPU コア

#### ステータスフラグ定数

| 定数 | ビット | 説明 |
|------|-------|------|
| `FLAG_CARRY`     | `0x01` | キャリーフラグ |
| `FLAG_ZERO`      | `0x02` | ゼロフラグ |
| `FLAG_INTERRUPT` | `0x04` | 割り込み禁止フラグ |
| `FLAG_DECIMAL`   | `0x08` | 10 進モードフラグ |
| `FLAG_BREAK`     | `0x10` | BRK フラグ |
| `FLAG_UNUSED`    | `0x20` | 常にセット（未使用） |
| `FLAG_OVERFLOW`  | `0x40` | オーバーフローフラグ |
| `FLAG_NEGATIVE`  | `0x80` | ネガティブフラグ |

#### クラス: `CPU`

#### レジスター

| 属性 | 初期値 | 説明 |
|-----|-------|------|
| `a` | `0x00` | アキュムレーター（演算に使用） |
| `x` | `0x00` | インデックスレジスター X |
| `y` | `0x00` | インデックスレジスター Y |
| `sp` | `0xFD` | スタックポインター（`0x01xx` ページ） |
| `pc` | `0x0000` | プログラムカウンター |
| `status` | `0x24` | プロセッサーステータス（`FLAG_UNUSED \| FLAG_INTERRUPT`） |

#### 公開メソッド

| メソッド | 説明 |
|---------|------|
| `reset()` | リセットベクター（`0xFFFC`–`0xFFFD`）から PC をロードし、SP・ステータスを初期化 |
| `step()` | 1 命令をフェッチして実行し、サイクル数を返す |
| `run(max_steps)` | `BRK` 命令に到達するか `max_steps` を超えるまでループ実行し、実行ステップ数を返す |

#### 実装済み命令一覧

| オペコード | ニーモニック | アドレッシング | 説明 |
|-----------|-------------|--------------|------|
| `0x00` | BRK | impl | 強制割り込み（実行停止のトリガー） |
| `0xEA` | NOP | impl | 何もしない |
| `0xA9` | LDA #imm | immediate | 即値をアキュムレーターにロード |
| `0xA5` | LDA zp | zero page | ゼロページから A にロード |
| `0xAD` | LDA abs | absolute | 絶対アドレスから A にロード |
| `0x85` | STA zp | zero page | A をゼロページに格納 |
| `0x8D` | STA abs | absolute | A を絶対アドレスに格納 |
| `0xA2` | LDX #imm | immediate | 即値を X にロード |
| `0xE8` | INX | impl | X をインクリメント |
| `0xCA` | DEX | impl | X をデクリメント |
| `0x4C` | JMP abs | absolute | 絶対アドレスへジャンプ |
| `0x20` | JSR | absolute | サブルーティン呼び出し（戻りアドレスをスタックに積む） |
| `0x60` | RTS | impl | サブルーティンから戻る |
| `0x78` | SEI | impl | 割り込み禁止フラグをセット |
| `0xD8` | CLD | impl | 10 進モードフラグをクリア |
| `0x9A` | TXS | impl | X をスタックポインターへ転送 |
| `0xBA` | TSX | impl | スタックポインターを X へ転送 |
| `0x48` | PHA | impl | A をスタックにプッシュ |
| `0x68` | PLA | impl | スタックから A にポップ |
| `0x69` | ADC #imm | immediate | 即値と A を加算（キャリー込み） |

未実装のオペコードに遭遇した場合は `NotImplementedError` が送出されます。

#### 内部ヘルパーメソッド

| メソッド | 説明 |
|---------|------|
| `_fetch_byte()` | PC から 1 バイト読み取り、PC をインクリメント |
| `_fetch_word()` | PC からリトルエンディアンで 2 バイト読み取り |
| `_read_word(addr)` | 指定アドレスからリトルエンディアンで 2 バイト読み取り |
| `_push_byte(value)` | スタックに 1 バイトプッシュ |
| `_push_word(value)` | スタックに 2 バイトプッシュ（高バイト → 低バイト の順） |
| `_pop_byte()` | スタックから 1 バイトポップ |
| `_pop_word()` | スタックから 2 バイトポップ |
| `_update_zn(value)` | ゼロフラグ・ネガティブフラグを値に応じて更新 |
| `_adc(value)` | ADC 命令の演算処理（キャリー・ゼロ・ネガティブフラグ更新） |

---

## コンポーネント間の関係

```
┌─────────────────────────────────────────────────────┐
│ __main__.py (CLI)                                   │
│  1. INESRom.load(path)  ──────────────────────────► │
│  2. Memory.with_rom(prg_rom)  ────────────────────► │
│  3. CPU(memory)  ─────────────────────────────────► │
│  4. cpu.reset() / cpu.run(max_steps)                │
└─────────────────────────────────────────────────────┘
         │              │              │
         ▼              ▼              ▼
    ┌─────────┐   ┌──────────┐   ┌──────────┐
    │INESRom  │   │ Memory   │◄──│  CPU     │
    │         │   │          │   │          │
    │prg_rom  │──►│  RAM     │   │ a,x,y    │
    │chr_rom  │   │  PRG ROM │   │ sp, pc   │
    └─────────┘   └──────────┘   │ status  │
                                  └──────────┘
```

---

## 実装の制限事項

| 機能 | 状態 |
|------|------|
| 6502 CPU（一部命令） | ✅ 実装済み |
| iNES ヘッダー解析 | ✅ 実装済み |
| 2 KB RAM + NROM マッピング | ✅ 実装済み |
| PPU（グラフィック） | ❌ 未実装 |
| APU（サウンド） | ❌ 未実装 |
| メモリーマップド I/O | ❌ 未実装 |
| 割り込み（IRQ/NMI） | ❌ 未実装 |
| すべての 6502 命令 | ❌ 一部のみ実装 |
| マッパー（NROM 以外） | ❌ 未実装 |

---

## 使い方

```bash
# 基本実行
python -m nes path/to/rom.nes

# 最大ステップ数を指定して実行
python -m nes path/to/rom.nes --steps 10000
```

実行が停止すると、実行ステップ数と以下のレジスター状態が出力されます：

- `A` — アキュムレーター
- `X` / `Y` — インデックスレジスター
- `SP` — スタックポインター
- `PC` — プログラムカウンター
- `STATUS` — プロセッサーステータスバイト
